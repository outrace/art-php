<?php
namespace cmd\page;

use cmd\base;
use now\dao;
use now\fun;

/**
 * 进行任务审核
 * @package cmd\user
 */
class check extends base {

    /**
     * 更改用户密码
     * @author 欧远宁
     * @param $para mixed
     */
    public function check($para) {

        $this->_ret(array());
    }

    /**
     * 获取待审清单
     * @param $para mixed
     */
    public function get($para){

    }

}
